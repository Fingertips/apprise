process do |files|
  specs = files.take_and_map do |file|
    case file
    when /^spec\/.+_spec\.rb$/
      file
    when /^lib.+?(\w+)\.rb/
      "spec/#{$1}_spec.rb"
    end
  end
  
  run_ruby_tests specs
end